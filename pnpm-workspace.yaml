# pnpm-workspace.yaml

packages:
  # Das '.' steht für das Haupt-Projekt (deine Next.js App)
  - '.'
  # Das hier ist dein Unter-Projekt für die Firebase Functions
  - 'firebase_functions'
  # Webmail-Proxy (läuft auf Hetzner Server)
  - 'webmail-proxy'

# Fix für Dependency-Konflikte zwischen whatsapp-web.js und Next.js
pnpm:
  overrides:
    # Stelle sicher dass ws kompatibel mit Next.js ist
    ws: "^8.2.3"
