<!DOCTYPE html>
<html lang="{{ mailcow_locale|default('en') }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Taskilo Mail - Login</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <link rel="icon" href="/img/taskilo-favicon.png" type="image/png">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: #f8fafc;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    
    .header {
      background: white;
      padding: 0 40px;
      height: 80px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0 2px 8px rgba(0,0,0,0.06);
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
    }
    
    .header-logo { display: flex; align-items: center; gap: 12px; }
    .header-logo img { height: 50px; }
    
    .header-nav { display: flex; align-items: center; gap: 30px; }
    .header-nav a {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-decoration: none;
      color: #0d9488;
      font-size: 12px;
      gap: 4px;
    }
    .header-nav a i { font-size: 24px; }
    .header-nav a:hover { color: #0f766e; }
    
    .main {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 100px 20px 60px;
    }
    
    .login-container { width: 100%; max-width: 500px; }
    
    .login-title-small {
      font-size: 18px;
      font-weight: 400;
      color: #1e293b;
      margin-bottom: 4px;
    }
    
    .login-title-big {
      font-size: 48px;
      font-weight: 800;
      color: #0d9488;
      margin-bottom: 40px;
      letter-spacing: -1px;
    }
    
    .form-group { margin-bottom: 16px; }
    
    .input-wrapper {
      display: flex;
      align-items: center;
      background: white;
      border: 2px solid #e2e8f0;
      border-radius: 8px;
      overflow: hidden;
      transition: all 0.2s;
    }
    
    .input-wrapper:focus-within {
      border-color: #0d9488;
      box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.1);
    }
    
    .input-wrapper i {
      padding: 16px 20px;
      color: #64748b;
      font-size: 20px;
    }
    
    .input-wrapper input {
      flex: 1;
      border: none;
      outline: none;
      padding: 16px 16px 16px 0;
      font-size: 16px;
      font-family: inherit;
    }
    
    .input-wrapper input::placeholder { color: #94a3b8; }
    
    .forgot-link {
      display: inline-block;
      margin: 12px 0 20px;
      color: #1e293b;
      text-decoration: underline;
      font-size: 14px;
    }
    .forgot-link:hover { color: #0d9488; }
    
    .checkbox-wrapper {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 30px;
    }
    .checkbox-wrapper input[type="checkbox"] {
      width: 20px;
      height: 20px;
      accent-color: #0d9488;
    }
    .checkbox-wrapper label { font-size: 14px; color: #1e293b; }
    
    .submit-btn {
      width: 100%;
      padding: 18px 32px;
      background: linear-gradient(135deg, #14b8a6 0%, #0d9488 50%, #0f766e 100%);
      border: none;
      border-radius: 50px;
      color: white;
      font-size: 16px;
      font-weight: 600;
      font-family: inherit;
      cursor: pointer;
      transition: all 0.3s;
      box-shadow: 0 4px 20px rgba(13, 148, 136, 0.35);
    }
    .submit-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 25px rgba(13, 148, 136, 0.45);
    }
    
    .footer {
      background: #f8fafc;
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 13px;
      color: #64748b;
      border-top: 1px solid #e2e8f0;
    }
    .footer-links { display: flex; gap: 20px; }
    .footer-links a { color: #64748b; text-decoration: none; }
    .footer-links a:hover { color: #0d9488; }
    
    .alert {
      padding: 12px 16px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 14px;
    }
    .alert-danger {
      background: #fef2f2;
      color: #dc2626;
      border: 1px solid #fecaca;
    }
    
    @media (max-width: 600px) {
      .header { padding: 0 20px; }
      .login-title-big { font-size: 36px; }
      .footer { flex-direction: column; gap: 15px; text-align: center; }
    }
  </style>
</head>
<body>
  <header class="header">
    <div class="header-logo">
      <img src="/img/taskilo-logo.png" alt="Taskilo">
    </div>
    <nav class="header-nav">
      <a href="https://taskilo.de/hilfe" target="_blank">
        <i class="bi bi-question-circle"></i>
        <span>Hilfe</span>
      </a>
      <a href="#" onclick="return false;">
        <i class="bi bi-globe"></i>
        <span>Deutsch</span>
      </a>
    </nav>
  </header>

  <main class="main">
    <div class="login-container">
      <p class="login-title-small">Taskilo Webmail</p>
      <h1 class="login-title-big">LOGIN</h1>
      
      <form method="post" autocomplete="off">
        <div class="form-group">
          <div class="input-wrapper">
            <i class="bi bi-envelope"></i>
            <input type="email" name="login_user" placeholder="E-Mail-Adresse" required autofocus autocomplete="username">
          </div>
        </div>
        
        <div class="form-group">
          <div class="input-wrapper">
            <i class="bi bi-lock"></i>
            <input type="password" name="pass_user" placeholder="Passwort" required autocomplete="current-password">
          </div>
        </div>
        
        <a href="/reset-password" class="forgot-link">Passwort vergessen</a>
        
        <div class="checkbox-wrapper">
          <input type="checkbox" id="remember" name="remember" checked>
          <label for="remember">Angemeldet bleiben</label>
        </div>
        
        <button type="submit" class="submit-btn">Anmelden</button>
      </form>
    </div>
  </main>

  <footer class="footer">
    <div class="footer-links">
      <a href="https://taskilo.de" target="_blank">taskilo.de</a>
      <a href="https://taskilo.de/agb" target="_blank">AGB</a>
      <a href="https://taskilo.de/datenschutz" target="_blank">Datenschutz</a>
      <a href="https://taskilo.de/impressum" target="_blank">Impressum</a>
    </div>
    <div>&copy; 2025 The Freelancer Marketing</div>
  </footer>
</body>
</html>
